services:
  web:
    build: .
    ports:
      - "4000:4000"
    environment:
      - FLASK_ENV=production
      - PORT=4000
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
    restart: always